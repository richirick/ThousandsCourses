<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.App.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/dark_gray</item>
        <item name="itemActiveIndicatorStyle">@style/App.ActiveIndicator</item>

        <!-- ✅ Правильные атрибуты для цветов -->
        <item name="itemIconTint">@color/nav_item_selector</item>
        <item name="itemTextColor">@color/nav_item_selector</item>

        <item name="labelVisibilityMode">labeled</item>
    </style>

    <style name="App.ActiveIndicator"
        parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/light_gray</item>
        <item name="android:width">64dp</item>
        <item name="android:height">32dp</item>
        <item name="shapeAppearance">@style/App.ActiveIndicator.Shape</item>
    </style>

    <style name="App.ActiveIndicator.Shape"
        parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>
</resources>